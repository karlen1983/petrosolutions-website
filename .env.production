# Public-only env values for production builds.
# This file IS committed to git (see .gitignore).
# DO NOT put secrets here — secrets live in `wrangler secret put` (Cloudflare)
# or GitHub Secrets (CI). Anything prefixed NEXT_PUBLIC_* is shipped to the
# browser bundle and is therefore public by definition.

NEXT_PUBLIC_TURNSTILE_SITE_KEY="0x4AAAAAAC8LMm3USLgQayfZ"
